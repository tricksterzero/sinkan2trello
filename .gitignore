# 設定ファイル（APIキー/トークンを含むため管理外）
config.js
config.js.bak

# 退避した過去のバックアップzip
_archive/

# Node（依存ゼロのため通常は生成されないが念のため）
node_modules/

# 依存ゼロのため lock する対象がなく、メタ情報のドリフト源になるので管理外。
# 将来依存を追加したら再生成してコミットすること。
package-lock.json
