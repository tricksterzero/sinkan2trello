# 改行コードの方針をリポジトリ側で固定する。
# これにより各マシンの core.autocrlf 設定に依存せず一貫した扱いになる。

# 既定: テキストはリポジトリ上 LF に正規化（自動判定）。
* text=auto eol=lf

# Windows バッチは CRLF を維持する（CRLF 前提で動くため）。
*.bat text eol=crlf

# バイナリは変換対象外（誤変換による破損を防ぐ）。
*.zip binary
